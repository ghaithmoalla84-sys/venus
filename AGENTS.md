# AGENTS.md — مشروع Venus (فينوس كوفي)

دليل السياق والأصول لوكيل الذكاء الاصطناعي عند العمل على هذا المشروع. اقرأه قبل أي تعديل.

## نظرة عامة
- **Venus** تطبيق محاسبة/إدارة متجر (قهوة) بـ Python + PyQt5 (واجهة عربية RTL).
- مجلد العمل: `D:\acc`. الكود المصدري في `venus/`، الاختبارات في `tests/`، مخطط البداية في `migrations/create_database.py`.
- الإصدار في `venus/__init__.py` (`__version__`).

## قواعد أمان صارمة (أولوية قصوى)
1. **لا تلمس قواعد البيانات الحية** `venus.db` / `venus_notes.db` أبداً. جرّب دائماً عبر قاعدة مؤقتة (انظر "الاختبارات").
2. **أسماء الجداول/الأعمدة كلها عربية** — انسخها من الاستعلامات القائمة حرفياً، لا تترجمها ولا تخترع أسماء.
3. **قفل اليومية المغلقة**: أي عملية تؤثر النقدية/المبيعات/المصروفات في يومية مغلقة (`أرصدة_الصندوق.مغلقة = 1`) ممنوعة. افحص عبر `SalesRepository().is_day_closed(date)` أو استعلام مباشر قبل الحفظ/التعديل/الحذف/التراجع، وأبلغ المستخدم برسالة إعادة الفتح من شاشة النقدية.
4. **المعاملات متعددة الجداول**: غلّفها بـ `BEGIN TRANSACTION` / `conn.commit()` / `conn.rollback()`، وأغلق الاتصال في `finally: conn.close()`.
5. **إشعار الواجهات**: بعد أي تغيير في البيانات استدعِ `from venus.core.events import app_events; app_events.emit_data_changed("<entity>")`. الكيانات الصالحة: `sales`, `purchases`, `materials`, `creditors`, `cash`, `expenses`, `withdrawals`, `groups`.
6. **تطبيع التواريخ**: حوّل الأرقام العربية إلى لاتينية قبل الإدراج: `s.translate(str.maketrans("٠١٢٣٤٥٦٧٨٩","0123456789"))`.
7. **الرصيد لا يُعدَّل يدوياً**: ديون/مخزون تتغيّر فقط عبر حركات موثّقة (`تحركات_الديون` بأنواع `إضافة`/`دفعة`، `تحركات_المخزون` بأنواع `شراء`/`تعديل_يدوي`/`جرد`).
8. **لا تستخدم `git push --force`** إطلاقاً.

## مخطط قاعدة البيانات (الجداول الأساسية)
| الجدول | ملاحظات/قيود مهمة |
|---|---|
| `المجموعات` | `الاسم` فريد؛ `المواد_الفرعية` ترتبط بها |
| `المواد_الفرعية` | `الوحدة` IN (`كيلوغرام`,`قطعة`,`لتر`)؛ `سعر_الشراء_الأخير`؛ `الحد_الأدنى` |
| `المخزون` | مفتاح=`معرف_المادة_الفرعية`؛ `الكمية_المتوفرة` |
| `تحركات_المخزون` | `نوع_الحركة` IN (`شراء`,`تعديل_يدوي`,`جرد`)؛ `الرصيد_بعد` |
| `فواتير_الشراء` | `المبلغ_الإجمالي`>0؛ `العملة` IN (`ليرة_سورية`,`دولار`) |
| `تفاصيل_الشراء` | بنود الفاتورة؛ `الكمية`>0، `سعر_الوحدة`>0 |
| `المبيعات_اليومية` | `معرف_المجموعة`؛ `نوع_المعاملة` DEFAULT `نقدي`؛ تُجمَّع حسب التاريخ+المجموعة |
| `الديون` | `نوع_الطرف` IN (`مورد`,`صديق`)؛ `حالة_الدين` IN (`نشط`,`مسدد`,`متأخر`)؛ الرصيد محسوب من الحركات |
| `تحركات_الديون` | `نوع_الحركة` IN (`إضافة`,`دفعة`) |
| `المصروفات` | `نوع_المصروف` IN (`إيجار`,`رواتب`,`كهرباء`,`ماء`,`نقل`,`أخرى`) |
| `السحوبات` / `الخزنة` / `تحويلات_الصندوق` | حركة النقدية؛ `من_حساب`/`إلى_حساب` IN (`الخزنة`,`الدرج`,`الخارجي`) |
| `أرصدة_الصندوق` | يحوي عمود `مغلقة` (قفل اليومية) و`العملة` |
| `الجرد` | `الكمية_النظري`/`الفعلي`/`فرق_الجرد` لجرد المخزون |
| `الإعدادات` | مفاتيح مثل `سعر_صرف_الدولار`، `اسم_المحل` |
| `أسعار_الصرف` | تاريخية لسعر الدولار |
| `سجل_العمليات_الأخيرة` | `نوع_العملية` IN (`بيع`,`مصروف`,`سحب`)؛ يدعم التراجع (`تم_التراجع`) |

العملة: `ليرة_سورية` أو `دولار`؛ سعر الصرف من `الإعدادات`/`أسعار_الصرف`.

## المستودعات (Repositories)
في `venus/core/repositories/` استخدمها لـ CRUD على الكيانات بدل SQL مباشر عند توفرها:
- `MaterialsRepository` (`create/update/delete/get_by_id`)
- `CreditorsRepository` (`create/update` — الرصيد عبر حركات فقط)
- `SalesRepository` (`get_by_date`, `is_day_closed`, `update/delete`)
- `GroupsRepository` (`create`)
- `financial.py` للعمليات المالية.

## الاختبارات
- التشغيل من `D:\acc\tests`: `python -m pytest -q` (أو ملف محدد). الإعداد في `tests/pytest.ini` و`tests/conftest.py`.
- **قاعدة مؤقتة آمنة**: fixture `temp_db` (autouse) توجّه `venus.core.database.DATABASE_PATH` إلى قاعدة بمخطط كامل بأسماء عربية — **لا تلمس `venus.db`**.
- مساعِدات جاهزة في `tests/fixtures/helpers.py`: `insert_group`, `insert_material`, `insert_creditor`, `insert_expense`, `insert_cash_day`.
- الحوارات تُصمت تلقائياً عبر fixture `_mock_qmessagebox`؛ حدد الحوارات بـ `unittest.mock.patch` (نمط `with patch.object(QDialog, 'exec_', return_value=QDialog.Accepted)`).
- مرّر fixture `qt_app` وأنشئ الشاشة مباشرة (مثل `InventoryScreen()`).

## أدوات المشروع (مجلد `.kilo/`)
متاحة لتسريع العمل المتكرر:
- **أوامر**: `/github-push` (رفع آمن)، `/test-run` (تشغيل pytest وتصليح)، `/db-maintain` (صيانة المخطط).
- **وكلاء فرعيون**: `github-pusher`, `test-runner`, `db-maintainer`.
- **مهارات** (تُحمَّل تلقائياً حسب الوصف أو عبر أداة المهارات): `release` (رفع إصدار)، `add-screen`، `add-report`، `add-inventory-operation`، `add-feature-test`، `add-creditor`، `add-expense`، `add-sale`.

عند طلب مهمة تطابق إحداها، استخدم المهارة/الأمر المعني واحترم قواعده.

## الاستثناءات في `.gitignore`
يستثني تلقائياً: `*.db`، `backups/`، `*.zip`، `project_backup_*/`، `*.bak`، `*.log`، `.coverage`، والبيئات الافتراضية. لا ترفع قواعد بيانات أو نسخاً احتياطية.
